export interface Profile {
    address: string;
    email: string;
    id: any;
    lasName: any;
    name: any;
}

export interface ProfileTopArtist {
    artista: string;
    cancion: string;
    genero: string;
    id: number;
}

export interface InformationPeople {
    nombre: string;
    numero: string;
}